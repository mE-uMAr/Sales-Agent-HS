# Copy to .env.local

# Where the FastAPI backend is listening.
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Must match WIDGET_PUBLIC_KEY in backend/.env.
# Public by design — it is sent from the browser and gates nothing secret.
NEXT_PUBLIC_WIDGET_KEY=pub_dev
